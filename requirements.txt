# ============================================
# 🎙️ Mongolian Fast-Whisper STT (Streamlit Cloud Stable)
# ============================================

# UI
streamlit==1.40.1
streamlit-audiorec==0.1.3

# Core Whisper runtime
faster-whisper==1.0.3
ctranslate2==4.6.0
transformers==4.45.2
sentencepiece>=0.1.99
huggingface-hub>=0.23.0

# Audio + math
soundfile==0.13.1
numpy>=1.26.4,<3.0
pillow==11.1.0        # ✅ Compatible with Streamlit <12

# CPU-safe Torch (PyTorch index AFTER PyPI)
--index-url https://pypi.org/simple
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.9.0+cpu
torchaudio==2.9.0+cpu

# Helpers
tqdm==4.66.5
platformdirs==4.5.0

# ⚠️ Removed PyAV (not needed and fails to build)
# av==12.3.0
