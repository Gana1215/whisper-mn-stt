# ============================================
# 🎙️ Mongolian Fast-Whisper STT (Cloud Stable)
# ============================================

# UI
streamlit==1.40.1
streamlit-audiorec==0.1.3

# Core Whisper runtime
faster-whisper==1.0.3
ctranslate2==4.3.1          # ✅ compatible with faster-whisper 1.x
transformers==4.45.2
sentencepiece>=0.1.99
huggingface-hub>=0.23.0

# Audio + math
soundfile==0.13.1
numpy>=1.26.4,<3.0
pillow>=10.0.0,<11.0

# CPU-safe Torch
torch==2.9.0+cpu
torchaudio==2.9.0+cpu

# Add the extra index AFTER PyPI, not before
--index-url https://pypi.org/simple
--extra-index-url https://download.pytorch.org/whl/cpu

# Helpers
tqdm==4.66.5
platformdirs==4.5.0

# Fix build issues with PyAV (used by faster-whisper)
av==12.3.0
