# ============================================
# 🎙️ Mongolian Fast-Whisper STT (Final Cloud Build)
# Works on Streamlit Cloud (Python 3.13)
# ============================================

# --- UI ---
streamlit==1.40.1
streamlit-audiorec==0.1.3

# --- Core Whisper Runtime ---
ctranslate2==4.6.0
transformers==4.45.2
sentencepiece>=0.1.99
huggingface-hub>=0.23.0
# ✅ safer, prebuilt version of faster-whisper
faster-whisper==0.10.1

# --- Audio + Math ---
soundfile==0.13.1
numpy>=1.26,<3.0
pillow>=10.3.0,<11.0      # ✅ streamlit-compatible & prebuilt
# ⚠️ no explicit "av" — faster-whisper 0.10.1 bundles runtime libs

# --- Torch CPU build ---
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.9.0+cpu
torchaudio==2.9.0+cpu

# --- Helpers ---
platformdirs==4.5.0
tqdm==4.66.5              # ✅ last PyPI + CPU mirror compatible version
