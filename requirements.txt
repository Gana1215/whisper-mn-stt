# ============================================
# 🎙️ Mongolian Fast-Whisper STT (Cloud Stable Fix)
# Works with Streamlit Cloud Python 3.13
# ============================================

# --- UI ---
streamlit==1.38.0
streamlit-audiorec==0.1.3

# --- Core Whisper Runtime ---
ctranslate2==4.6.0
transformers==4.45.2
sentencepiece>=0.1.99
huggingface-hub>=0.23.0
faster-whisper==1.0.3

# --- Audio + Math ---
soundfile==0.13.1
# don't pin an exact numpy version – let Streamlit pick the wheel it can fetch
numpy>=1.26,<3.0
# remove PyAV entirely to avoid pkg-config build
# av==12.0.0
pillow==10.2.0

# --- Torch CPU build ---
torch==2.9.0+cpu
torchaudio==2.9.0+cpu
--extra-index-url https://download.pytorch.org/whl/cpu

# --- Helpers ---
platformdirs==4.5.0
tqdm>=4.67.0
