# ============================================
# 🎙️ Mongolian Fast-Whisper STT (v2.3 — Cloud-Stable Mobile Fast)
# ============================================

# --- UI ---
streamlit==1.40.1
# streamlit-audiorec==0.1.3   # ❌ Not needed since we now use st.audio_input()

# --- Core Whisper Runtime ---
faster-whisper==1.2.0
ctranslate2==4.6.0
transformers==4.45.2
sentencepiece>=0.1.99
huggingface-hub>=0.23.0
streamlit-audiorec

# --- Audio + Math ---
soundfile==0.13.1
numpy>=1.26.4,<3.0
scipy==1.14.1
pillow>=11.0.0  # ✅ relaxed version pin for uv resolver
av==16.0.1      # ✅ new: PyAV (for WebM/Opus decode, replaces pydub+ffmpeg)

# --- CPU Torch ---
torch==2.9.0
torchaudio==2.9.0

# --- Helpers ---
tqdm==4.66.5
platformdirs==4.5.0
